# dependencies
node_modules/
# build output
dist/
frontend/dist/
# scratch / staging
.stage/
# sync tarballs (used to move src/dist between machines)
*.tgz
# logs
*.log
npm-debug.log*
# os / editor
.DS_Store
.vscode/
vite.config.js.timestamp-*.mjs

# scratch build folders (cannot be deleted from the cloud sandbox)
_to_delete/
# obsidian workspace
.obsidian/
# screenshot build folders
.shot*/
